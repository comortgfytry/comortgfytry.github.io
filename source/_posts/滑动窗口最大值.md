---
title: 滑动窗口最大值
tags:
  - 算法
  - 子串
categories: [算法,子串]
keywords: 算法 子串
date: 2023-07-31 10:59:11
---

# 题目

给你一个整数数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 k 个数字。滑动窗口每次只向右移动一位。

返回 滑动窗口中的最大值 。

输入：nums = [1,3,-1,-3,5,3,6,7], k = 3  
输出：[3,3,5,5,6,7]

# 分析
* 直接遍历，但超出时间限制
```java
class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        int len = nums.length;
        List<Integer> tmp = new ArrayList<>();
        for (int i = 0; i < len - k + 1; i++) {
            int max = nums[i];
            for (int j = i; j < i + k - 1; j++) {
                max = max < nums[j+1] ? nums[j+1] : max;
            }
           tmp.add(max);
        }
        return tmp.stream().mapToInt(Integer::valueOf).toArray();
    }
}
```

* 随后想到可以判断向右移动时是否失去最大数来减少循环中遍历，但还是部分超出时间限制

```java
 int len = nums.length;
        int max = nums[0];
        List<Integer> tmp = new ArrayList<>();
        for (int i = 0; i < k - 1; i++) {
            max = max > nums[i+1] ? max : nums[i+1];
        }
        tmp.add(max);
        for (int i = 0; i < len - k; i++) {
            if (nums[i+k] > max) {
                max = nums[i+k];
                tmp.add(max);
            }
            else if (max == nums[i]){
                max = nums[i+1];
                for (int j = i+1; j < i + k; j++) {
                    max = max > nums[j+1] ? max : nums[j+1];
                }
                tmp.add(max);
            }else tmp.add(max);
        }
        return tmp.stream().mapToInt(Integer::valueOf).toArray();
```
* 最后发现可以使用优先队列  
原因：能够自动排序(可以按从小到大排列)

* 再优化:使用单调队列(满足单调性的双端队列)  
从末尾放入元素，队列从左到右依次递减
可以节省将元素放入队列中的时间，因为优先队列将一个元素放入的时间复杂度为O(logn)    
有关队列的知识[点击跳转](队列.md)

# 解法

优先队列
```java
class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        int len = nums.length;
        int[] res = new int[len - k + 1];
        // 优先队列里node节点为一个长为2的数组(nums的一个值，对应索引)
        PriorityQueue<int[]> pq = new PriorityQueue<>((o1, o2) -> o1[0] != o2[0] ? o2[0] - o1[0] : o2[1] - o1[1]);
        for (int i = 0; i < k; i++) {
            pq.offer(new int[]{nums[i], i});
        }
        res[0] = pq.peek()[0];
        for (int i = k; i < len; i++) {
            pq.offer(new int[]{nums[i],i});
            while (pq.peek()[1] <= i - k) {
                pq.poll();
            }
            res[i - k + 1] = pq.peek()[0];
        }
        return res;
    }
}

```

单调队列

```java
class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        int len = nums.length;
        int[] res = new int[len- k + 1];
        Deque<Integer> deque = new LinkedList<>();
        for (int i = 0; i < k; i++) {
            while (!deque.isEmpty() && nums[deque.peekLast()] < nums[i]){
                deque.pollLast();
            }
            deque.offerLast(i);
        }
        res[0] = nums[deque.peekFirst()];
        for (int i = k; i < len; i++) {
            while (!deque.isEmpty() && nums[deque.peekLast()] < nums[i]) {
                deque.pollLast();
            }
            deque.offerLast(i);
            while (deque.peekFirst() <= i - k) {
                deque.pollFirst();
            }
            res[i - k + 1] = nums[deque.peekFirst()];
        }
        return res;
    }
}

```